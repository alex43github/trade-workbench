name: S6.1 Forward Observer

on:
  push:
    branches:
      - feature/s6-1-forward-observer

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.13.0'
          cache: npm
      - run: npm ci
      - name: Targeted S6.1 tests
        id: targeted
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          node --experimental-strip-types --test tests/execution-forward-observer.test.mjs 2>&1 | tee s61-targeted.log
      - name: Full repository tests
        id: full_tests
        if: always()
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          NODE_OPTIONS=--experimental-strip-types npm test 2>&1 | tee s61-full-tests.log
      - name: Typecheck
        id: typecheck
        if: always()
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npx tsc --noEmit 2>&1 | tee s61-typecheck.log
      - name: Final build
        id: build
        if: always()
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm run build 2>&1 | tee s61-build.log
      - name: Diff check
        id: diff_check
        if: always()
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          git diff --check 2>&1 | tee s61-diff-check.log
      - name: Static safety audit
        id: safety
        if: always()
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          {
            test -d services/execution-forward
            if grep -R -nE 'binance-gateway|placeOrder|submitOrder|createOrder|services/structure-radar/main' services/execution-forward; then
              echo 'Forbidden trading/runtime dependency found' >&2
              exit 1
            fi
            grep -R -n 'NO_TRADING_ACTIONS = 1' services/execution-forward
          } 2>&1 | tee s61-safety.log
      - name: Upload verification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: s61-verification-${{ github.sha }}
          path: s61-*.log
          if-no-files-found: error
          retention-days: 7
      - name: Verification gate
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          printf 'targeted=%s\n' '${{ steps.targeted.outcome }}'
          printf 'full_tests=%s\n' '${{ steps.full_tests.outcome }}'
          printf 'typecheck=%s\n' '${{ steps.typecheck.outcome }}'
          printf 'build=%s\n' '${{ steps.build.outcome }}'
          printf 'diff_check=%s\n' '${{ steps.diff_check.outcome }}'
          printf 'safety=%s\n' '${{ steps.safety.outcome }}'
          test '${{ steps.targeted.outcome }}' = success
          test '${{ steps.full_tests.outcome }}' = success
          test '${{ steps.typecheck.outcome }}' = success
          test '${{ steps.build.outcome }}' = success
          test '${{ steps.diff_check.outcome }}' = success
          test '${{ steps.safety.outcome }}' = success
