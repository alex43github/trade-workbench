# REQ-20260908-change-log-protocol

状态：VERIFIED_LOCAL  
创建时间：2026-09-08  
最后更新时间：2026-09-08  
是否部署到 VPS：否（本次是项目文档和流程协议变更）

## 用户原始要求

用户希望今后在项目下不同对话中提出修改和升级要求时，每个需求都在项目根目录下留下可追踪日志，使后续模型能够读取历史要求、功能升级历史和实际完成状态，核对哪些已经成功上线、哪些只是口头声称完成、哪些仍需继续处理。

## 范围与验收标准

- 新增一个可直接复制使用的后续模型提示词；
- 规定每个需求创建唯一 REQ 日志；
- 规定日志状态、验收证据、VPS 部署证据和敏感信息禁止项；
- 提供日志规范、日志索引和本次需求的第一条日志；
- 将协议与当前 VPS 交接文档关联起来；
- 不修改业务代码，不触发 VPS 部署。

## 修改前基线

- 已有 `VPS-HANDOFF.md`，但没有独立的跨对话变更索引和强制状态定义；
- 原有 `handoff.md` 记录了历史背景，但不能单独证明每次需求是否实际部署和验证；
- 当前工作区存在前序未提交改动，不能通过回滚方式整理。

## 实施计划

1. 新增 `PROJECT-MODIFICATION-PROMPT.md`，定义后续模型的强制流程。
2. 新增 `change-logs/README.md`，定义日志格式和状态流转。
3. 新增 `change-logs/INDEX.md`，提供跨对话检索入口。
4. 为当前协议需求创建本日志。

## 实际修改

- `PROJECT-MODIFICATION-PROMPT.md`
- `change-logs/README.md`
- `change-logs/INDEX.md`
- `change-logs/REQ-20260908-change-log-protocol.md`

## 本地验证

- `git diff --check`：通过。
- 日志中未写入 API Key、Secret、Token、Cookie 或 SSH 私钥。
- 当前变更仅为 Markdown 文档和日志协议，未运行构建或业务测试。

## VPS 部署与线上验证

本次不涉及代码发布，不涉及服务重启，不涉及线上验证。因此不能标记为 `DEPLOYED_UNVERIFIED` 或 `VERIFIED_ON_VPS`。

## 当前状态

已在本地项目中建立跨对话变更审计协议，可供后续模型读取和续写。

## 剩余问题与下一步

- 下一次真实代码修改必须遵守该协议，并在开始改代码前创建新的 REQ 日志。
- 如果后续模型忘记创建日志，用户可以要求它先补齐当前需求日志，再继续实施。

## 时间线

- 2026-09-08：创建项目级修改提示词、日志规范、日志索引和本次需求日志。

