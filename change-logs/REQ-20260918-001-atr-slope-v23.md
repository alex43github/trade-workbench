# REQ-20260918-001-atr-slope-v23

状态：INTEGRATED_VERIFIED
创建时间：2026-09-18 00:00 Asia/Shanghai
最后更新时间：2026-09-18 00:00 Asia/Shanghai

## 用户原始要求

执行唯一任务 `TASK C-ATR-SLOPE-V23-001`：修正 C5/C3/C1 ATR 持续榜、Focus Pool 与 Bark 展示；保持 D 最新规格；完成本地验证、提交推送、VPS 同步和线上验收。

## 范围与验收标准

- C5/C3/C1 仅接受对应 streak >= 3 的已收盘 1H K 线，历史每根使用自身 MA30 与 Wilder/RMA ATR14。
- C 各级独立按 log-normalized Slope20 降序，稳定 tie-break；Bark 各级 Top10，Focus 各级 Top20 后按 symbol 合并并保留来源/排名。
- D-LONG Top10 Bark/Top20 Focus，D-SHORT Top10 Bark/Top10 Focus；稳定币基准排除；不新增全市场扫描或交易路径。
- 相关测试覆盖用户列出的准入、独立集合、排序、容量、来源、Slope20 和排除规则；运行相关测试、typecheck/build。
- 仅同步本任务相关文件；不触碰真实订单、交易网关或密钥。

## 修改前基线

- 本地工作基线：`2e6e8a43127f40008c2d8a0b9048f85e05939e90`，分支 `source-reconcile/20260913-vps`；根目录存在与本任务无关及 ATR 相关的 dirty/untracked work，未修改。
- 集成基线：远端 `origin/main` `d55d0c88ae1271dfec8a7b6d0cf2fe0c7ec91b64`，未证明与 VPS 等价。
- VPS：`/opt/trade-workbench` 非 Git worktree，`RELEASE.json` 缺失；相关 C/Focus/Bark 源码与静态 systemd timer 存在。
- 线上相关 unit 初始状态：`trade-workbench-atr-persistence-v1.timer=active`、`trade-workbench-focus-pool-v22.timer=active`、`trade-workbench-focus-v22-cd-bark.timer=active`；主站、squeeze-radar、binance-gateway active。

## 并行开发与集成

- 开发基线版本：未建立可信发布版本；以当前 `HEAD` 为只读恢复基线。
- 开发基线 commit：`2e6e8a43127f40008c2d8a0b9048f85e05939e90`
- 当前 main commit（开始时）：`99e374be905298dfee3e876a027c43a4d8c88c4a`
- 工作分支和 worktree：`task-REQ-20260918-001-atr-slope-v23` / `.worktrees/task-REQ-20260918-001-atr-slope-v23`
- 可能冲突的 REQ / 文件 / 数据契约：线上 VPS-only C/Focus/Bark 文件；根目录未提交 ATR persistence 初始实现；不修改 Gateway、订单、数据库、timer 定义或 Production Model。
- 最终集成 main commit：`fa4d482`（基于远端 main `d55d0c8` cherry-pick）。
- rebase/merge 结果：cherry-pick 仅遇到 `change-logs/INDEX.md` modify/delete 文档冲突；遵循最新 main 保持该文件删除，任务 REQ 记录保留。
- 语义冲突检查：任务相关测试 44/44 通过，build 通过；C/D 输出契约与 Bark 入口已验证，未修改交易路径。

## 实施计划

1. 在隔离 worktree 重现并测试 VPS C/Focus/Bark 代码。
2. 以最小改动统一 ATR 计算、C 分榜、Focus 并集和 Bark 结构，补充测试。
3. 运行相关测试、typecheck/build，检查只包含任务文件的 commit。
4. 集成到最新本地 main、推送 main；使用 VPS 发布锁同步任务文件，DRY_RUN 后复用产物做一次 C/D 生成链验收。

## 实际修改

- 新增共享 `slope20.ts`，统一使用 `[ln(MA30_now)-ln(MA30_20bars_ago)]/20`。
- 更新 ATR persistence：已收盘 1H K、逐根 MA30 与 Wilder/RMA ATR14、streak>=3 准入、输出 Slope20。
- 更新 Focus Pool：C5/C3/C1 独立 Top20/Top10，按 Slope20 排名；最终按 symbol 合并并保留多级来源；D 维持 LONG 20/10、SHORT 10/10。
- 更新 Bark：C 一条消息内 C5/C3/C1 三个区块，D 保持 LONG/SHORT 两个区块；生成脚本不新增扫描。
- 新增/更新相关测试，覆盖准入、独立集合、Slope 排名、容量、来源保留、稳定币排除及 Bark 结构。

## 本地验证

- `node --test tests/c-atr-slope-v23.test.mjs tests/atr-persistence-v1.test.mjs tests/focus-pool-v22.test.mjs tests/radar-bark-batching.test.mjs tests/bark-alerts.test.mjs tests/structure-radar-bark.test.mjs`：44/44 通过。
- `npm run build`：通过，退出码 0。
- `npx tsc --noEmit`：退出码 1；最新 main 集成后仅报告主线既有错误（`app/api/radar/atr-band/route.ts`、`app/trade/TradingTerminal.tsx`、`lib/radar/ma30-*`、`lib/telegram/*`、`lib/trade/*`），未涉及本任务文件。
- 根工作区 dirty/untracked 清单未修改；本任务所有变更仅在隔离 worktree。

## VPS 部署与线上验证

待完成；部署时仅同步本任务代码文件，禁止读取或记录任何环境变量值、密钥、Token、Cookie 或 Authorization header。

## 当前状态

INTEGRATED_VERIFIED；待 push main 及 VPS 线上验收。

## 剩余问题与下一步

- VPS 缺少发布 manifest，线上版本只能以同步后的任务文件 hash、相关 unit 状态和产物验收作为证据。
- 需完成 push、任务文件 hash 核对、一次复用现有 ATR 产物的 C/D 生成链及 Bark DRY_RUN 预览。

## 时间线

- 2026-09-18：完成只读基线核对，建立隔离 worktree，导入任务点名的远端源码。
- 2026-09-18：完成任务相关本地实现与测试/build 验证；typecheck 仅剩既有无关错误。
- 2026-09-18：基于最新远端 main 完成集成，集成测试 44/44 通过、build 退出码 0；全量 typecheck 仍被主线既有错误阻塞，错误未涉及任务文件。
