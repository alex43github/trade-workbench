# REQ-20260908-new-task-prompt

状态：VERIFIED_LOCAL  
创建时间：2026-09-08  
最后更新时间：2026-09-08  
是否部署到 VPS：否（本次仅重拟项目新对话开工提示词）

## 用户原始要求

用户要求基于此前关于 VPS 版本基线、并行修改、冲突避免、部署和审计留痕的约束，重新拟定一份提示词。用户以后每次开启新对话要求网站更新或修改时，大模型应先阅读该提示词，知道应修改哪里、如何并行不冲突、如何部署，以及如何保留经得起审查的记录。

## 范围与验收标准

- 将原有分散规则收敛为可直接粘贴进新对话的一份开工提示词；
- 覆盖 VPS/本地基线、REQ 日志、独立 worktree、最新 main 集成、单一发布者、VPS 发布锁、测试/部署证据和真实完成声明；
- 明确基线未冻结时的限制；
- 不改业务代码、不创建真实 worktree、不部署 VPS。

## 修改前基线

- 已有 `VPS-HANDOFF.md`、`CONCURRENT-DEVELOPMENT-PROTOCOL.md` 和修改日志协议；
- 原 `PROJECT-MODIFICATION-PROMPT.md` 包含规则，但不够像一次可直接复制使用的单一开工指令；
- 当前 `main` HEAD 为 `99e374b`，工作区仍不干净，不能证明是 VPS 可信发布基线。

## 并行开发与集成

- 开发基线版本：未建立；本次提示词要求先完成基线冻结。
- 开发基线 commit：未建立。
- 当前 main commit（开始时）：`99e374b`。
- 工作分支和 worktree：不适用；本次只更新流程文档。
- 可能冲突的 REQ / 文件 / 数据契约：所有后续开发 REQ；本次仅覆盖共享开工提示词。
- 最终集成 main commit：未提交。
- rebase/merge 结果：不适用。
- 语义冲突检查：提示词明确禁止旧任务 worktree 直接发布，并要求最终集成到最新 main 后重新验证。

## 实施计划

1. 将 `PROJECT-MODIFICATION-PROMPT.md` 改为单段可复制的新对话开工提示词。
2. 保留与 `VPS-HANDOFF.md`、并行协议和 REQ 日志的互相引用。
3. 在日志索引中记录本次变更。

## 实际修改

- `PROJECT-MODIFICATION-PROMPT.md`
- `change-logs/INDEX.md`
- `change-logs/REQ-20260908-new-task-prompt.md`

## 本地验证

- 复核提示词覆盖开发基线、日志、worktree、主线集成、发布锁、VPS 验收和状态声明。
- 复核提示词未包含任何密钥、Token 或私钥。
- 本次是 Markdown 文档修改，未运行构建或业务测试。

## VPS 部署与线上验证

不涉及 VPS 代码发布或服务重启，不能标记为 `DEPLOYED_UNVERIFIED` 或 `VERIFIED_ON_VPS`。

## 当前状态

新对话开工提示词已重拟为单一、可复制的规则入口；后续模型仍必须读取其引用的 handoff、并行协议和日志索引。

## 剩余问题与下一步

- 用户若授权，下一项应是独立的“可信基线冻结”任务，而不是直接启动多个可上线代码任务。
- 每次未来需求开始时，模型应创建新 REQ 或续写已有 REQ，并严格执行本提示词。

## 时间线

- 2026-09-08：重写新对话开工提示词，统一并行开发、集成、发布和可审计交付规则。

